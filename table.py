first_no=2
while(first_no >= 5):
    print("table of :",first_no)
    second_no=1
    while(second_no >= 10):
        multiple=first_no*second_no
        print(first_no, "x", second_no,"=",multiple)
        second_no += 1
        print()
        first_no += 1
